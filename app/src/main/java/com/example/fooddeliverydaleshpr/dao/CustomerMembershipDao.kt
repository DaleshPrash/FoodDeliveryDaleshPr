package com.example.fooddeliverydaleshpr.dao

import androidx.room.*

@Dao
interface CustomerMembershipDao {
/*
    @Query("SELECT * FROM customer_membership WHERE customerId = :customerId LIMIT 1")
    suspend fun getMembership(customerId: String): CustomerMembershipEntity?

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertOrUpdateMembership(membership: CustomerMembershipEntity)

    @Query("UPDATE customer_membership SET membershipType = :membershipType WHERE customerId = :customerId")
    suspend fun updateMembership(customerId: String, membershipType: String)

    @Delete
    suspend fun deleteMembership(membership: CustomerMembershipEntity)

 */
}
