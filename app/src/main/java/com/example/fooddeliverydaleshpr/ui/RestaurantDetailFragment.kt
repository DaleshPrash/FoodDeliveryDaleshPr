package com.example.fooddeliverydaleshpr.ui

import androidx.fragment.app.Fragment
import com.example.fooddeliverydaleshpr.R

class RestaurantDetailFragment : Fragment(R.layout.fragment_restaurant_detail) {
/*
    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)

        // Set up any data or event listeners here
        // For example, handle clicks on the delivery options

        view.findViewById<RadioGroup>(R.id.orderOptions).setOnCheckedChangeListener { _, checkedId ->
            when (checkedId) {
                R.id.radioDelivery -> { /* Handle Delivery option */ }
                R.id.radioPickup -> { /* Handle Pickup option */ }
                R.id.radioGroupOrder -> { /* Handle Group Order option */ }
            }
        }
    }

 */
}
